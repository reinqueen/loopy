# Shared client library

The shared library wraps the partner-facing REST API for JavaScript consumers. It follows API versions and can be released independently.

Its current documentation covers installation and retries, but not compatibility promises for new status meaning.
