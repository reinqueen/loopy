# REST API

The API exposes shipment status to the portal and selected partners. Several status fields are inconsistently described, and partner compatibility expectations are not documented.

Endpoint inventory and test commands are intentionally omitted from this fixture.
