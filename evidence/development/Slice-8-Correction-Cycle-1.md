# Slice 8 Correction Cycle 1

## Basis

Independent evaluation of exact candidate `ff297d5c4bea3728637b649110944a64f4d6dc24` failed on four material Slice 8 boundaries. The UI Host attributed an unrelated operated target to the candidate, published non-resolving evidence links, recorded disclosed simulated input as personal human judgment, and could not construct valid Demo CLI payloads from the normal frozen skill route without inspecting implementation sources. The deterministic Engine focus run otherwise preserved rejection non-mutation and atomic Complete-but-unreleased behavior.

## Focused correction

- Demo authority now names the expected operated target. Every observation must provide an exact target-verification claim backed by passing, independently inspectable evidence. A mismatch rejects before mutation with Demo retry/reauthorization/block guidance and no inferred Build defect.
- Workspace evidence is existence- and hash-validated, then stored relative to nested `Demo.md`. Durable URI evidence is HTTPS; ephemeral references remain visibly inaccessible and cannot support demonstrated coverage.
- Genuine-human, simulated-participant, and proxy-AI input are explicit. Simulated/proxy input with `personalJudgment: true` rejects; truthfully declared advisory input remains separate from Human judgment and cannot complete the Milestone.
- The normal Engine reference now includes the supported runtime invocation and links complete Demo operation payloads. Hosts need not inspect TypeScript, tests, or internal contract YAML.

## Evidence boundary

Focused tests prove formal validation, routing, rendering, and byte-stable rejected-operation behavior. They do not prove that a Host correctly identifies a real target, chooses sufficient target-verification evidence, assesses semantic candidate behavior, or maintains the provenance distinction in an unsteered conversation. Fresh independent UI and non-UI Host runs remain required. Slice 8 implementation is not human-accepted, and Slice 6 remains independently passing but not human-accepted.

## Verification

The frozen offline install completed without changes. The full suite passed **184 tests in 9 files**; TypeScript no-emit checking and the production build passed; and the official validator reported `Skill is valid!`. All **11 YAML files** and **1 frontmatter block** parsed, **97 local links across 93 Markdown files** resolved, and `git diff --check` passed. The tests include workspace containment and atomic rejected-operation non-mutation. No source `.loopy` exists. Clean-worktree status is recorded after the correction commit.
