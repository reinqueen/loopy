# Slice 7 Implementation Evidence

## Candidate boundary

This record covers the bounded implementation of the human-approved [Milestone Build and Prove](../../MILESTONE-BUILD-AND-PROVE-EXPERIENCE.md) definition. It preserves the single Loopy skill and stops before Demonstration, human Milestone Acceptance, completion, Release, push, merge, deployment, publication, or open-ended external mutation.

## Builder implementation

The implementation adds canonical Build state, exact lifecycle and source/workspace anchors, Work Unit-scoped active-session grants, provider-neutral task assignments with actual model provenance, sequential and explicit-safe-parallel work tracking, Plan-owned evaluation-budget activation, frozen candidate and correction lineage, independent neutral-unproven evaluation records, append-only evidence/history, and a readable `Proof.md`. The Engine records declared work and inert evidence; it does not execute target-product commands or external-system mutations.

Ready for Demonstration requires a PASS from the mandatory final integrated independent boundary on the exact frozen Milestone Build Candidate plus complete supported declared coverage for every committed acceptance criterion and planned check. This remains distinct from semantic Proof judgment, Demo, Confirmation, human Acceptance, completion, and Release.

Controlled automated coverage includes missing/overbroad authority, source-control separation, exact workspace/source reuse, required order, explicit safe parallelism, task/model provenance, unverified-model labeling, private state versus published views, builder/evaluator separation, exact charters, incomplete final coverage, zero-limit exhaustion, explicit budget increases, failed-result preservation, correction lineage, Plan drift revalidation, stale resume, transaction rollback, CLI routing, one next action, and prior lifecycle regressions.

## Evidence boundary

Builder tests and scenarios prove deterministic state, validation, rendering, containment, and rollback behavior. They do not prove that a Host chooses good implementation steps, makes a sound model recommendation, recognizes every semantic conflict, creates genuinely independent tasks, or that recorded evidence semantically proves the outcome. The independent multi-turn scenario remains required for the unproven conversational cases described below. Slice 6 remains independently passing but not human-accepted.

## Builder verification

The final builder run passed 158 automated tests across eight suites, TypeScript no-emit typecheck and production build, official skill validation, 10-file YAML parsing, 82-file Markdown local-link checks, frozen dependency installation, and working-tree whitespace/source-boundary checks. The lockfile install completed from the local store; pnpm's optional update lookup emitted a non-failing offline metadata warning. No `.loopy` project state exists in the Loopy source.

## Correction cycle 1

Independent static evaluation of exact candidate `230f4a6` reproduced four blocking Engine defects: failed raw evidence could support a final PASS; prior builder evidence could substitute for exact-candidate final-evaluator evidence; an eligible later unordered Work Unit could bypass sequential-by-default Plan order; and a second builder could silently replace an active Work Unit owner. The evaluator classified generic npm packaging as later Release scope, so this correction does not change it.

The correction validates evidence truth and exact final-evaluator/candidate provenance before accepting supported final coverage, enforces stable first-eligible execution unless an explicit safe parallel group applies, and rejects silent active-owner replacement. Four focused adversarial regressions preserve canonical state, history, `LOOPY.md`, `Roadmap.md`, and `Proof.md` byte-for-byte on rejection. This is builder correction evidence, not a replacement for independent reevaluation or human implementation acceptance.

Correction verification passed 162 automated tests across eight suites, TypeScript no-emit typecheck and production build, official skill validation, 10-file YAML parsing, 82-file Markdown local-link checks, frozen dependency installation, and working-tree whitespace/source-boundary checks. The install again completed from the lockfile and local store with only pnpm's non-failing optional update-lookup warning.

## Final bounded builder correction

A broader independent follow-up of exact `e9f0b48` confirmed correction cycle 1 but preserved four remaining concerns for the one authorized final builder attempt: assignment during interruption or after the readiness stop; redundant active Work Unit assignments; an unchanged correction candidate; and the inability to represent a fresh evaluator task/context in a separate clean disposable workspace. This correction changes only those guards and evaluator provenance. It does not revisit the already-passing evidence, ordering, lifecycle, or packaging boundaries.

Assignments now require the exact active Build session and active phase, incomplete Work Units reject a second active builder assignment, and a correction candidate must differ from its failed parent in source or workspace fingerprint before lineage is written. Evaluator assignments retain the exact implementation source/workspace anchors while separately recording a fresh task/context identity and a distinct clean disposable evaluation workspace; `Proof.md` publishes that provenance. Focused tests preserve state, history, `LOOPY.md`, `Roadmap.md`, and `Proof.md` byte-for-byte on every rejected operation. At this point these were builder results awaiting independent reevaluation and human acceptance.

Final-attempt verification passed 165 automated tests across eight suites, TypeScript no-emit typecheck and production build, official skill validation, 10-file YAML parsing, 82-file Markdown local-link checks, frozen dependency installation, and working-tree whitespace/source-boundary checks. The lockfile install completed locally with the same non-failing optional pnpm update-lookup warning.

## Independent reevaluation and human acceptance

Fresh independent reevaluation returned **PASS** for exact candidate `e55cec14a70e182e11145bc22a5aebe6b716902d`. It reran all five requested adversarial groups, the valid exact-candidate path, 165 automated tests, typecheck, production build, skill validation, YAML and link checks, diff checks, and source-containment checks in clean/disposable evaluator workspaces. On 2026-09-07, the human explicitly accepted that evaluated implementation as recorded in [Slice 7 Human Acceptance](Slice-7-Human-Acceptance.md).

The human explicitly delegated a proxy human-perspective review of the preserved customer-recovery walkthrough to the orchestrating Host AI, received its assessment and limitations, and then explicitly accepted Slice 7. The human did not personally review or perform the walkthrough. Because the AI-operated scenario supplied most execution authority in its opening request, the ordinary conversational experience of gathering permissions gradually remains a non-blocking deferred UX walkthrough. This does not claim Demonstration, Milestone outcome Acceptance, completion, Release, publication, deployment, merge, push, Slice 6 human acceptance, or release qualification.
