# Slice 6 Implementation Evidence

## Candidate boundary

This record covers the bounded implementation of the human-approved [Milestone Planning and Conditional Guides](../../docs/design/stages/MILESTONE-PLANNING-EXPERIENCE.md) definition. It preserves the single Loopy skill and stops before implementation, Build, Proof execution, Demonstration, Release execution, worktree creation, publication, or deployment.

## Builder verification

The implementation carries committed Definitions into independently versioned Plans, keeps the committed boundary authoritative, records outcome-first work units and typed dependencies, distinguishes canonical interview state from published views, supports conditional reusable guides with responsibility-bounded confirmation, and maintains one project next action. Planning readiness is routing only and grants no later-stage authority.

Controlled automated coverage includes the golden handoff, independent blocked and ready Plans, guide timing and confirmation, dependency-cycle rejection, view staleness and resume repair, synchronized Active lifecycle rendering, atomic rollback, CLI and single-skill routing, and regressions for Slices 1–5. During development, guide confirmation initially failed to append Plan history when it changed a blocked Plan's phase; that was corrected. The Roadmap also initially retained the prior Planned lifecycle description after Planning began; synchronized lifecycle rendering and regression coverage correct that contradiction.

The final builder run passed 129 automated tests across seven suites, the TypeScript production build, official skill validation, nine-file YAML parsing, repository Markdown link checks, frozen dependency installation, and working-tree/candidate-diff whitespace checks. The source contains no `.loopy` project state. The dependency command completed from the lockfile and local store; its optional registry update-notice lookup reported an offline metadata warning without failing installation.

## Evidence boundary

Automated and builder-authored scenarios prove deterministic contracts and rendering, not whether a Host asks the best questions, decomposes work well, recognizes semantic boundary changes, or judges parallel safety correctly. The independent multi-turn suite in `evaluation/scenarios/milestone-planning/interview-quality/` remains required. Slice 6 implementation remains unaccepted until a later explicit human decision.

## Independent evaluation and focused correction

Independent evaluation of exact candidate `589446a4235ea8c09d87726d425ee0d657b0dbd1` returned **FAIL** despite a behavioral Host pass. It reproduced two Engine defects: a `safe` parallel claim could contradict a direct or transitive required-before path, and Planning could publish or continue after captured Product, Engineering, Roadmap, and Definition-state revisions advanced.

The focused correction cross-checks every safe pair in both directions against the required-before transitive closure. It also rejects all Planning mutation, review, guide confirmation, resume, render, status, and readiness/handoff paths when any foundation anchor differs. The error exposes changed anchors; only explicit Host semantic revalidation against the exact current revisions updates provenance, appends history, and clears prior Plan review readiness. Earlier lifecycle writers no longer silently rewrite stale Planning state. Generated revisit wording was also normalized without changing lifecycle semantics.

Regression tests cover both dependency directions, transitive paths, all four foundation anchors, rejected-operation non-mutation, operation-path rejection, an actual meaning-preserving Product correction that advances all synchronized foundations, wrong-anchor rejection, successful explicit revalidation, and readable revisit text. A fresh independent correction evaluation is still required; this builder record does not claim it passed or accept Slice 6.

The focused correction builder run passed 132 automated tests across seven suites, the TypeScript production build, official skill validation, nine-file YAML parsing, repository Markdown link checks, frozen dependency installation, and working-tree plus candidate-diff whitespace checks. The source contains no `.loopy` project state. As before, the lockfile install completed from the local store while an optional package-manager update lookup emitted a non-failing offline metadata warning.

## Portable dependency-map refinement

Before implementation acceptance, the human requested a presentation-only refinement. `Roadmap.md` and each applicable `Plan.md` now render compact fenced plain-text dependency maps from the same canonical typed relationships used by their detailed tables and parallel sections. Maps use readable names, stable existing ordering, no internal identities, and disappear entirely when no relationship exists. No state field, lifecycle transition, authority boundary, or additional human artifact was added. Automated coverage checks required, helpful, independent, safe, and blocked semantics; map/table agreement; omission; ordering; readable names; and stale-view regeneration. This builder verification remains distinct from Slice 6 human acceptance.

The refinement builder run passed 134 automated tests across seven suites, TypeScript typecheck and production build, official skill validation, YAML and local-link checks, frozen dependency installation, and whitespace/source-boundary checks. The package-manager's optional update lookup again emitted a non-failing offline metadata warning after the lockfile install completed.

Independent evaluation of dependency-map candidate `5c6a2cca8c222aa65602d4786f58d6cf31b2a142` returned **FAIL** on one presentation defect: Planning publication replaced the Roadmap range that contained the committed Definition dependency map. Canonical relationships remained intact, but the human Roadmap became incomplete.

The focused correction gives the Roadmap dependency map one compositional renderer. After all lifecycle overlays, it removes any prior generated map and deterministically regenerates one section from current typed Living Roadmap and Milestone Definition relationships. This avoids duplicates, stale generated sections, and dependence on renderer order; Plan-map behavior and lifecycle semantics are unchanged. End-to-end regression now carries Roadmap and committed Definition dependencies through Planning initialization, private update, guide review and confirmation, Plan review, status, foundation revalidation, stale-view repair, pause, and resume while checking one map, one copy of each relationship, detailed Definition agreement, and one project next action. A fresh independent correction evaluation remains required; this builder evidence does not claim human acceptance.

The correction builder run passed 135 automated tests across seven suites, TypeScript no-emit typecheck and production build, official skill validation, nine-file YAML parsing, 74-file Markdown local-link checks, frozen installation, and whitespace/source-boundary checks. The install completed from the lockfile and local store; its optional package-manager update lookup emitted the same non-failing offline metadata warning.

Focused reevaluation of correction `e6f5a2fbe1e44a12c61df86b5959dcb04df64113` closed the Planning composition defect but returned **FAIL** on one remaining presentation issue: a dependency inherited normally from a selected Roadmap candidate into its Definition appeared twice because Roadmap and Definition renderers used different phrases for the same required relationship.

The second focused correction deduplicates typed relationships before formatting, using normalized source, target, kind, boundary, and note as the semantic key. The single human phrase is `required before <boundary>`. Relationships with different kinds or boundaries remain distinct; same-endpoint records with materially different notes remain visible with Roadmap or Definition provenance rather than being silently collapsed. Regression coverage uses normal candidate-to-Definition inheritance, verifies one map line and section against canonical and Definition-table state through the full Planning publication matrix, and separately verifies preservation of conflicting detail. No state, lifecycle, or authority changed. A fresh independent reevaluation remains required, and this evidence does not claim human acceptance.

The correction builder run passed 137 automated tests across seven suites, TypeScript no-emit typecheck and production build, official skill validation, nine-file YAML parsing, 74-file Markdown local-link checks, frozen installation, and whitespace/source-boundary checks. The optional package-manager update lookup remained a non-failing offline warning.

Final focused reevaluation of `0a4131d7b64a240b44c9a719ae68c75d90c04a68` returned **FAIL** on a deeper cross-stage cause: Milestone Definition inheritance retained dependency name, boundary, and note but discarded the Roadmap relationship kind. The renderer therefore had no truthful basis for distinguishing inherited required, helpful, and independent meaning and synthesized every Definition dependency as required-before.

The focused correction adds only the missing typed `kind` to Definition dependency state and its formal contract, copies it unchanged during Slice 3→4 initialization, validates it before writes, and shows it in the readable Definition dependency table. The composed Roadmap renderer now uses that canonical kind, deduplicates unchanged inherited relationships semantically, preserves different notes and boundaries, and explicitly marks same-endpoint/boundary kind differences for review rather than choosing one silently. No new state machine, operation, approval, responsibility, artifact, or downstream authority was added. Invalid kinds are rejected without state, history, or view mutation.

End-to-end coverage carries required-before, helpful/non-blocking, and independent/potentially-parallel relationships through normal Roadmap selection, Definition inheritance and commitment, Planning initialization and every previously covered update, review, guide-confirmation, status, revalidation, stale-repair, pause, and resume publication boundary. It checks one map section, one unchanged inherited line, typed Definition-table agreement, stable distinct/conflicting display, and one project next action. A fresh independent reevaluation remains required; this builder evidence does not claim human acceptance.

The correction builder run passed 139 automated tests across seven suites, TypeScript no-emit typecheck and production build, official skill validation, nine-file YAML parsing, 74-file Markdown local-link checks, frozen installation, and whitespace/source-boundary checks. The optional package-manager update lookup remained a non-failing offline warning.

## Safety-focused relationship summaries

Before Slice 6 implementation acceptance, the human approved a final presentation refinement. `Plan.md` now derives a compact **Delivery safety** view from the existing canonical work-unit relationships: numbered required-before adjacency flows support chains, branches, convergence, multiple roots, and disconnected chains; only explicit safe claims appear under **Can happen in parallel**; and blocked claims appear under **Needs coordination** with their basis and conflicts. Helpful and merely independent dependencies remain in the detailed sections and are not presented as safety claims.

`Roadmap.md` now presents required ordering, canonical independent relationships as **Potentially parallel**, and unresolved or conflicting meaning as **Needs coordination**. It deliberately does not describe portfolio independence as safe implementation concurrency. Both summaries are deterministic generated views with no new writable diagram state, artifact, authority, or lifecycle transition. Coverage exercises omission, readable names, stable ordering, DAG shapes, safe-only parallel display, blocked reasons, helpful exclusion, inherited relationship deduplication, detail agreement, stale regeneration, and the full Planning publication matrix. This is builder evidence only; a fresh independent human-readable evaluation and later explicit Slice 6 implementation acceptance remain separate.

The final builder run passed 140 automated tests across seven suites, TypeScript no-emit typecheck and production build, official skill validation, nine-file YAML parsing, 74-file Markdown local-link checks, frozen installation, and working-tree plus candidate-diff whitespace checks. The optional package-manager update lookup emitted its known non-failing offline metadata warning after the lockfile install completed.

## Plan-owned Build and evaluation boundaries

Before Slice 6 implementation acceptance, the human requested a narrow cross-stage refinement. Planning now records zero or more risk-justified focused independent checkpoints and exactly one required final integrated independent evaluation boundary. Each boundary carries readable scope, type, trigger/applicable Work Units, risk basis, required state, and a nonnegative correction-cycle limit. The limit means the maximum corrected new candidates allowed after an initial failed evaluation, with reevaluation required for each; zero is valid. Plan readiness requires this structurally coherent budget, while review remains routine and grants no Build or evaluation activation authority.

The Engine stores this budget in existing Plan content, orders focused checkpoints by applicable Work Unit order and the final boundary last, renders one compact table, rejects invalid kinds, references, duplicate finals, optional finals, and negative limits before writes, and preserves independent Plans plus stale-view regeneration. It does not infer whether risk warrants a focused checkpoint and adds no Build state machine. Automated tests distinguish a low-risk zero-focused Plan from a missing final boundary, exercise zero/nonzero limits, ordering, readable rendering, invalid-budget non-mutation, multi-Plan independence, and repair of a stale Plan view. This remains builder evidence, not Slice 6 human implementation acceptance or Slice 7 approval.

The refinement builder run passed 145 automated tests across seven suites, TypeScript no-emit typecheck and production build, official skill validation, nine-file YAML parsing, 75-file Markdown local-link checks, frozen installation, and working-tree whitespace/source-boundary checks. The lockfile install completed from the local store; its optional package-manager update lookup emitted a non-failing offline metadata warning.

Independent evaluation of exact candidate `bc6bfc6607b16c2748fa58b8465138559422425c` returned **FAIL** on two focused findings despite the other budget behavior and both blind Host scenarios passing. First, a Host-declared coherent update could persist an empty evaluation budget, and a caller-supplied final boundary followed by a focused checkpoint was silently reordered before validation. Second, the Planning experience overstated current durable state by claiming that context scale and speed-versus-quality or cost preference were always recorded.

The correction validates the resulting Host-declared coherent Plan budget before normalization: exactly one required final integrated independent boundary must already be the caller-supplied last entry. Missing and final-not-last submissions now reject before state, history, or view writes, while intentionally incomplete draft updates remain permitted. Focused checkpoints may still be normalized deterministically ahead of the correctly supplied final boundary. Byte-identical regressions cover both reproduced failures and the `planning-apply` route.

The documentation now states only what current Slice 6 can represent: existing Work Units, risks, dependencies, external effects, and evaluation-boundary strength provide inputs to later model choice. The proposed Slice 7 derives a tentative assessment from those inputs and current availability, asks only for genuinely missing Human preferences, and selects the exact provider/model at task assignment. No Slice 6 field or Slice 7 implementation was added. A fresh independent correction evaluation remains required; this evidence does not claim Slice 6 human acceptance or Slice 7 definition approval.

The correction builder run passed 146 automated tests across seven suites, TypeScript no-emit typecheck and production build, official skill validation, nine-file YAML parsing, 75-file Markdown local-link checks, frozen installation, and working-tree whitespace/source-boundary checks. The lockfile install completed from the local store; its optional package-manager update lookup emitted a non-failing offline metadata warning.
